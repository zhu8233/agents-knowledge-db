# 本地覆盖说明

This directory is for local differences relative to the system snapshot.

Do not copy whole system files here unless there is no safer alternative.

Use `mcp-access-policy.json` for local MCP identity mapping and tool exposure rules.
