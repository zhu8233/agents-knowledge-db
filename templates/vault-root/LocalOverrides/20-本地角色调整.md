# 本地角色调整

Use this file only for local role or authority differences from the system snapshot.

For MCP-facing auth and tool exposure, prefer `mcp-access-policy.json` over editing the registry directly.
